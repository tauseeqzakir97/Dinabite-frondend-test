import styled from "styled-components";

export const TopNavContainer = styled.div`
  h2 {
    color: #999a9d;
    padding: ${(props) => props.StylePro};
  }

  hr {
    border: none;
    height: 1px;
    width: 100%;
    background: #e5e5e5;
    max-width: 900px;
    margin: 0 auto;
  }
`;
